# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust workspace
target/

# Local-only cargo config (dev paths override redirecting strands-rs to a sibling clone)
.cargo/config.toml

# Yarn (using yarn.lock)
.yarn/
.pnp.*

# Local env files — Sentry DSNs etc. Use `.env.example` as the
# committed reference for variable names; never commit real values.
.env
.env.*
!.env.example
